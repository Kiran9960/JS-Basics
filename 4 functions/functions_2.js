console.log(" **** --- Calculation 1 -- *****"); //divider

function calculation1(valueA, valueB) {
  let addition = valueA + valueB;
  let multiplication = valueA * valueB;
  let division = valueA / valueB;
  let subtraction = valueA - valueB;

  return {
    addition: addition,
    multiplication: multiplication,
    division: division,
    subtraction: subtraction,
  };
}
var result = calculation1(12, 8);

console.log("addition " + result.addition); // 20
console.log("subtraction " + result.subtraction); // 4
console.log("multiplication " + result.multiplication); //96
console.log("division " + result.division); //1.5

console.log("******************* "); //divider

/////////////// ************************** ///////////////////

function calculation(valueA, valueB) {
  let addition = valueA + valueB;
  let multiplication = valueA * valueB;
  let division = valueA / valueB;
  let subtraction = valueA - valueB;
  
  
  return addition, multiplication, division, subtraction;
}
var additionResult = calculation(5, 3);
var subtractionResult = calculation(5, 3);
var multiplicationResult = calculation(5, 3);
var divisionResult = calculation(5, 3);
var subtractionResult = calculation(5, 3);

console.log("Addition result: " + additionResult);
console.log("Subtraction result: " + subtractionResult);
console.log("Multiplication result: " + multiplicationResult);
console.log("Division result: " + divisionResult);
console.log("Subtraction result: " + subtractionResult);

/////////////// **************************
//Write a function that takes a string as a parameter
//and returns the length of the string.
///////////////////

console.log(" **** --- String length -- *****"); //divider

function whatTheLength(yourString) {
  let = stringLength = yourString.length;

  return stringLength;
}

myString = whatTheLength("Kiran");
console.log(myString);

console.log(" **** --- String length -- *****"); //divider

/////////////// **************************
// Implement a function that takes an array of numbers as
// a parameter and returns the largest number in the array.
///////////////////

function bigNumber() {

}
